Заменить в проекте файл:
pages/projects/[projectId]/results.tsx

Причина падения:
в предыдущей версии useMemo для analysisLayout был объявлен после условных return, что ломало порядок React hooks и валило страницу client-side exception.

В этой версии:
- сохранена новая раскладка блока «Итоговый аналитический вывод»
- исправлен порядок hooks
- убраны дубли одинаковых индексных секций
