Что изменено

Файл: lib/commercialEvaluation.ts

Сделано точечно:
- карточки компетенций в AI+ больше не собираются из одной общей фразы типа "зона риска / опора"
- в каждую карточку добавлен понятный контур:
  1) краткий вывод
  2) какие тесты реально повлияли
  3) сигнал за
  4) сигнал против
  5) почему такой балл
- если срабатывает AI по кастомному промту, первая часть карточки теперь берётся из него,
  поэтому карточки лучше различаются между компетенциями
- если AI не вернул текст, включается детерминированный fallback по тестам

Заменить можно только этот файл.
